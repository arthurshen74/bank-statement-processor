import React from 'react';
import { Outlet } from 'react-router-dom';

// This component is no longer used directly - see Layout.jsx for main app structure
const App = () => {
  return <Outlet />;
};

export default App;
